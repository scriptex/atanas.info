checks:
    - name: build-size-watcher
      options:
          files:
              - path: dist/**/*.*
    - name: commit-deployment
      options:
          buildPath: ./dist
    - name: type-coverage-watcher
      options:
          atLeast: 90
          ignoreCatch: true
