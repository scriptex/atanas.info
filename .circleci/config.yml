version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor:
      name: node/lts
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: yarn
            - run: yarn prod
workflows:
    build-and-test:
      jobs:
        - build-and-test
