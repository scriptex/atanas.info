version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:lts
      - image: circleci/php:7.1-node-browsers
    steps:
      - checkout
      - run:
          name: install
          command: yarn install
      - run:
          name: prod
          command: yarn prod
